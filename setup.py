from setuptools import find_packages, setup
setup(
    name='cydflib',
    packages=find_packages(include=['cydflib']),
    version='0.1.0',
    description='My first Python library',
    author='Me',
    license='MIT',
    install_requires=[],
    setup_requires=[],
    tests_require=[],
    test_suite='tests',
)
